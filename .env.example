# BirthdayDrop — Environment Variables
# Copy this to .env and fill in your values.
# Never commit your real .env to version control!

# ── MongoDB ──────────────────────────────────────────────────
# Local MongoDB (default):
MONGO_URI=mongodb://127.0.0.1:27017/birthdaydrop

# MongoDB Atlas (cloud) — replace with your connection string:
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/birthdaydrop?retryWrites=true&w=majority

# ── Express Server ───────────────────────────────────────────
PORT=5000
FRONTEND_URL=http://localhost:5173

# ── Vite Frontend ────────────────────────────────────────────
VITE_API_URL=http://localhost:5000
